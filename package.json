{
  "name": "base16-default-dark-theme",
  "version": "3.7.23",
  "scripts": {
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "release:patch": "npm --tag-version-prefix='' version patch && git push && git push --tags",
    "release:minor": "npm --tag-version-prefix='' version minor && git push && git push --tags",
    "release:major": "npm --tag-version-prefix='' version major && git push && git push --tags",
    "prepare": "husky",
    "build": "node scripts/build.mjs",
    "check:parity": "node scripts/check-parity.mjs",
    "test:normalize": "node scripts/test-normalize.mjs",
    "inventory": "node scripts/inventory.mjs --check",
    "reload": "node scripts/reload.mjs",
    "shots": "node scripts/shots.mjs",
    "format": "prettier --write \"src/**/*.css\"",
    "lint": "stylelint \"src/**/*.css\"",
    "lint:fix": "stylelint \"src/**/*.css\" --fix"
  },
  "lint-staged": {
    "src/**/*.css": [
      "prettier --write",
      "stylelint --fix"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3",
    "stylelint": "^17.8.0",
    "stylelint-config-standard": "^40.0.0"
  }
}
