// Tokens listed here are intentionally public API for snippets or plugins and
// therefore may have no var() consumer inside src/. Keep this list empty when
// possible. Every exemption must name a declared token and explain its external
// consumer; check-dead-tokens.mjs rejects stale or duplicate entries.
export const DEAD_TOKEN_EXEMPTIONS = [];
